# 2026-08-14 全面审计（P1-P12 生存 + 创造/管理员 + 地形）

## 审计方式
4 路并行 Explore 子代理（P1-P2 / P3-P4 / P5+P7 / P8+P10）+ 我亲自审计其余（P6 熔炼附魔酿造、P9 存储、P11 种植、P12 成就、创造/管理员、地形）。完整报告 docs/audit_2026-08-14.md。

## 修复（22 项）
- **CRITICAL 红石坐标 key 编码位冲突**：RedstoneEngine 旧编码 `x<<32|z&0xFFFFFFFF|y<<16` 的 z 高 16 位被 y 覆盖（z<0 全失效）。统一改原版 BlockPos 编码 `(x&0x3FFFFFF)<<38|(z&0x3FFFFFF)<<12|(y&0xFFF)`（posKey/keyX/keyZ/keyY 工具方法，含 BFS visited/scheduledUpdates/trackedObservers/trackedPlates）。
- **HIGH 箭/末影珍珠打不中玩家**：findEntityHit 只遍历实体 Map；ArrowEntity/EnderPearlEntity 加玩家命中（含射手免疫 life<5、护甲结算、击退）。
- **HIGH 牛奶无效**（foodValue=0 不进进食分支）→ USE ITEM 加 milk_bucket 分支（clearEffects+返还桶）。
- **HIGH 金苹果缺效果** → 金苹果 regeneration+absorption；附魔金苹果加 resistance/fire_resistance。
- **HIGH 死亡经验公式** level×7 → (int)(totalXp*7/100)。
- **HIGH 堆叠全 64** → BlockManager 从 json/1.21.11/items.json 加载 stackSize，getMaxStackSize 查表（末影珍珠/箭 16、桶/药水 1）。
- **HIGH 末地紫颂树不生成** → 新增 ChorusTreeFeature（外岛 end_stone 表面，2-5 节主干+分支+花），placeFeatures THE_END 分支接入（seedLo+chunkX*341873128712L+chunkZ*132897987541L+98765L）。
- **HIGH 末影之眼不消耗** → 放框架时生存模式扣物品。
- **成就 P12**：4→25 个（全分支）；AdvancementDef 加 matchMobs；AdvancementTrigger 加 ENCHANT_ITEM/CONSUME_ITEM；接线 onCraftItem/onEnchantItem/onConsumeItem（合成/附魔/进食处）。
- **熔炼**：花朵 XP 1.0→0.1（8 种）、kelp 移出烟熏炉、dried_kelp_block 4001→4000。
- **酿造**：poison/regeneration 0:45、slow_falling 1:30、治疗+发酵蛛眼=伤害、岩浆膏抗火。
- **种植**：地狱疣 1/3（原版）、可可豆支撑改水平、竹子 12 高。
- **存储**：潜影盒可打开（打开判定+标题+ContainerStore.refAt）。
- **红石**：活板门受红石控制（isRedstoneConnectable+处理段）。
- **实体**：怪物经验表（仅 blaze 10）、怪物攻击不击退玩家（原版）。
- **PVP 暴击**加 !sprinting；**游泳饥饿消耗** 0.01/tick。
- **末影龙经验**：直接发放 → 掉落经验球（spawnExperienceOrbs 12000）。
- **javac_args.txt 重建**：自动从 src/main/java 生成（find 全量），避免新类漏编译。

## 重要发现/结论
- 子代理报告里 H5（怪物无敌帧 10 vs 20 tick）**有意保留 10 tick**（用户嫌怪难打，20 tick 玩家 DPS 减半）。环境伤害频率（10 vs 20 tick）同理由。
- 0x25 卸载坐标 X/Z 顺序正确（子代理误报）；heightmap 偏移正确（VerifyWithVanilla 覆盖负坐标）。
- 末地龙 dragonKilled return 阻止重复击杀经验（当前无水晶重生，无矛盾）。

## 遗留（详见报告"遗留问题"）
- **末地船鞘翅**（模板不放置 entities + 船 chest 无 LootTable → 生存拿不到鞘翅）——最优先。
- 末影龙水晶重生（onEndCrystalPlaced 死代码）、区块包 BlockEntity 数组、红石（活塞缩回/火把弱充能/漏斗吸掉落物/音符盒）、流体 5 tick 延迟、下界末地群系多样性、末影龙战斗细节、掉落表细节。

## 回归
SurvivalBot 13/13 全 PASS、合成 fuzz 50840 全 PASS、194 文件编译 0 错误、服务器 0 异常。

## 晚间大修复（全部验证通过）
- **区块包 BlockEntity 数组**（0x2C）：ChunkEncoder 从空列表改为真实 BE。typeId 用 blockEntityTypeId() 注册表映射（furnace=0/chest=1/.../sign=7/barrel=27/lectern=30/jigsaw=32）。**关键坑：tag 必须用 writeAnonymousNbt（type+entries+TAG_End），writeNbt 尾部多一个 0x00 会在 BE 连续数组里每项错位 1 字节**。VerifyBE（Bootstrap 完整注册表 + getBlockEntitiesTagsConsumer）leftover=0、BE 内容正确解码。
- **结构 data markers**：verify 时发现测试工具未调 BlockStateHelper.init()（假"方块全 0"）。末地船 VerifyMarkerDirect PASS：鞘翅展示框/潜影贝×3/战利品箱×2。鞘翅框 = marker(6,5,7)+origin 平移，facing 原版默认 SOUTH=3。
- **红石**：普通活塞缩回不拉方块（仅粘性）；顶障碍不伸头（extendPiston 返回 boolean，失败不置 extended）；火把弱充能（只强充能上方，侧面仅红石线/中继器/比较器可收）；漏斗吸上方掉落物；音符盒右键调音（note+1 mod 25 + playNoteManual）；比较器 1 tick（2 游戏刻）延迟（scheduledUpdates "comparator"）；观察者垂直放置。
- **末影龙**：end_crystal 物品放置生成 EndCrystalEntity + onEndCrystalPlaced（四水晶重生）；龙蛋右键瞬移；黑曜石平台 33x33（原版 84-116/-16-16）；**水晶重建只在 pendingCrystalRestore（服务器重启恢复）时执行——无条件重建会让玩家破坏的水晶复活，龙永远被保护**；**buildSpikes 重建前必须 crystalIds.clear()（否则幽灵水晶累积 hasLivingCrystals 恒 true）**。
- **掉落**：ghast +music_disc_tears；女巫按原版 1-3 rolls×6 材料 + redstone。
- **流体**：内部级联调度加 5 tick 延迟（FLOW_DELAY_TICKS=5）；玩家倒水 spreadSource 即时。
- **下界群系**：computeColBiome 按 chunk hash 分配 5 群系（nether_wastes=34/soul_sand_valley=49/crimson_forest=7/warped_forest=59/basalt_deltas=2）。BiomeTagResolver 的 BIOME_NAMES 恰好=注册表 id 顺序（已验证 0 mismatch）。nether_fossil 现在能生成。

## 最终回归（2026-08-14 深夜）
- **LateGameBot 27/27 全 PASS**（合成/熔炉烧炼/黑曜石传送门/下界往返/要塞/末影龙 10 水晶+BossBar+破坏水晶+部件攻击+击杀+终末诗+12000 经验 0→68 级）
- SurvivalBot 13/13 PASS；服务器 0 异常。
- 测试要点：末影龙在祭坛 (0,65,0)，玩家从黑曜石平台 (100,50,0) 距离 100>TRACK_RANGE=64 看不到——需 tp 到主岛；bot 判定龙死用终末诗 title 包（removedEntities 会被 tp 进出 64 格误触发）；攻击龙每次攻击前 tp 到 0x23 实时位置（近战 3.5 格 + 冷却衰减伤害 ~1.55/次，临界 200 血需高命中）。
