# 2026-08-07 工作记录

## 收尾 Bug3 / Bug10 / P0 全部项（用户: "bug3和bug10和p0的所有一起推进，完成后别关机了"）

### 编译结果
- 全部改动 `mvn -o compile -DskipTests` BUILD SUCCESS（177 源文件）。
- 编译中途报错：`NetworkHandler.java` 5144 与 5146 重复 `}`（cauldron 分支插入时与原 `} else if (bed)` 重复，把 if-else 链提前闭合）。修复：删除 5146 行多余的 `{`，保留 5144 闭合 cauldron，`else if (bed)` 接续链。再次编译通过。

### P0① 挖掘 harvest + 精准采集/时运（已完成）
- `PlayerData`：新增 `inventoryEnchants[46]`（Map<Integer,Integer> 槽位→附魔等级）、构造函数初始化、getter `getSlotEnchant(slot, id)`。
- `BlockManager`：新增 `blockHarvestTools`/`enchantNameToId`/`fortuneableBlocks` 加载，`canHarvest`/`getEnchantId`/`isFortuneable`。
- `NetworkHandler` 破坏方块(0x28)：`canHarvest` 闸门（无正确工具不掉）；`silk_touch`→掉方块本身；`fortune`→对 fortuneable 矿增数 `1+rand(fortune+1)`。

### P0④ 盔甲/盾牌/保护附魔（已完成）
- `damagePlayer` 加 overload `(damage, source, srcX, srcZ)`；PvP(`Interact` type==1) 改为 `target.damagePlayer(dmg,"player",x,z)`，路由经盔甲减伤；`MobEntity` 近战也带坐标。
- 盾牌：`isBlocking` 字段；举盾(`use_item` shield)、落盾(`RELEASE_USE_ITEM`/挥手)；`isBlockingFrom` 朝向锥判定；`getShieldSlot`/`isShield`。
- EPF 减伤：盔甲减伤后按 `protection/fire_protection/blast_protection/projectile_protection` 再减（cap 20 → /25），按伤害源类型叠加对应保护。

### P0② 窒息（已完成）
- `tickSurvival` 头部方块 `isSolidOpaque` 且非水/岩浆 → `damagePlayer(1.0,"suffocate")`。

### P0③ 末地浮空岛（已完成，前次会话）
- 重写 `EndIslandDensityFunction` 原版 `getHeightValue` 算法；`minValue=-0.84375, maxValue=0.5625`；`WorldManager.CHUNK_GEN_VERSION=2` 触发旧末地区块自修复。

### Bug 10 右键交互缺口（已完成）
- 空桶收岩浆（`use_item_on` 水源分支扩展）。
- 末影箱玩家维度存储：`ContainerStore.enderChest(UUID)`；`NetworkHandler.openEnderChests`；容器读写/点击/关闭助手均支持 ender。
- 比较器切模式、堆肥桶（概率+COMPOST_CHANCE 表）、炼药锅（水桶/岩浆桶/玻璃瓶装取/空桶抽水的 level 推进）、右键点燃（flint 分支，前次会话）。
- 漏斗 GUI（已完成，本次）：`ContainerStore.HopperData`（5 槽→`int[10]`）；`NetworkHandler` 新增 `openHoppers` 映射 + `getHopperContents`/`hopperToPlayerSlot` 助手；`handleBlockInteraction` 加 `hopper` 分支（menu-type=15=vanilla `hopper`，Window Items 发 41 格：5 漏斗+36 玩家）；`getSlotItem`/`setSlotItem`/`sendSlotUpdate`/shift-click/关闭/破坏掉落均接入漏斗。**menu-type 权威来源**：`RegistryHelper.sendMenuRegistry` 自定义清单不可信（箱子=2 实际按 vanilla generic_9x3 渲染），故一律用 vanilla 协议 id。

### Bug 3 locate/结构原版化（locate 路由已完成）
- `DensityRouterChunkGenerator.getColumnBiome(blockX,blockZ)`：按需构建 BiomeManager 供任意坐标查覆盖层生物群系。
- `NetworkHandler./locate` 重写：路由到 `StructureSet` 真实 `random_spread` 放置数学（`RandomSpreadStructurePlacement.isStructureChunk`），按玩家当前维度做生物群系合法性校验（`getGenerator(currentDim).getColumnBiome` + `BiomeTagResolver`）。名称解析支持单/复数及结构 id 子串匹配（village→villages，nether_fortress→nether_complexes 等）。原调用旧的死代码 `structure.StructureManager.findNearest` 已删除。
- 非拼图结构生成（已完成，本次）：新增 `structure2/NonJigsawPlacer.java`。`placeStructures2` 原 `if (!isJigsaw) continue` 改为分流——jigsaw 走 `JigsawPlacement`，其余走 `NonJigsawPlacer.place(...)`。`NonJigsawPlacer` 覆盖用户点名的 6 类 + 2 个模板类：
  - 程序化：`buried_treasure`（沙滩下埋宝箱，loot=`chests/buried_treasure`）、`mineshaft`（y8-44 隧道雕刻+铁轨+蛛网+偶尔矿车宝箱，`chests/abandoned_mineshaft`）、`stronghold`（简化：11x11x7 石砖房 + 中央 5x5 末地传送门外框）。
  - 模板散布：`shipwreck`(seaLevel-3)、`ocean_ruin`(→`underwater_ruin` 目录随机变体, surfaceY-1)、`ruined_portal`(随机变体, surfaceY-1)、`igloo`(bottom+middle+top 堆叠)、`nether_fossil`(→`fossil` 目录)。模板经 `StructureTemplate.load(变体路径)` → `placeInWorld(level,...)`，写入路径与 jigsaw 一致（`WorldGenLevel.setBlock`，窗口含 3x3 邻居）。
  - `pickVariant(dir,random)` 从 `mapping/.../data/minecraft/structure/<dir>/` 随机取 .nbt（已确认 shipwreck/underwater_ruin/ruined_portal/igloo/fossil 资产齐全）。
  - 未覆盖的非拼图类型（desert_pyramid/jungle_pyramid/swamp_hut/end_city/ocean_monument/woodland_mansion）：default 分支跳过，仍 /locate 可定位但不生成——留作后续。

### 本轮编译
- `mvn -o compile -DskipTests` BUILD SUCCESS（178 源文件）。新增 `NonJigsawPlacer.java`。

---

## 用户反馈 4 个新 bug + 补 6 类结构（已完成，本次会话）

### 已包含的 3 个包修复（前次会话写入，本次一并编译通过）
- 漏斗 menuType：0x39 open_screen 发 `writeVarInt(17)`（hopper 在 `RegistryHelper.sendMenuRegistry` 的真实序号，非 vanilla 15）。
- 熔炉家族 menuType：furnace=8 / blast_furnace=2 / smoker=23（按 registry 真实序号）。
- 攻击生物：0x29 在 1.21.11 实为 entity_event（Int+Byte），服务端改发 `writeInt(eid)`+`writeByte(2)`（Hurt），消除客户端断线。

### Bug 3 船无法放置（已完成）
- `NetworkHandler.use_item_on`(0x3F) 新增 `_boat` 物品分支：在 faceOffset 目标位生成通用 `Entity`（设 `typeName=物品名` 如 `oak_boat`），若目标方块是水则扫描水面把船放在水面上。生存模式递减物品。
- 船是独立实体类型（非 metadata 变体），无需额外元数据即可正确渲染。

### Bug 4 刷怪蛋无法使用（已完成）
- `use_item_on`(0x3F) 新增 `_spawn_egg` 分支：剥掉 `_spawn_egg` 后缀得到 mob 名 → `MobEntity`。全部 87 个刷怪蛋已验证后缀即实体名，且均在 `data/entities.json` 中。

### EntityManager.entityTypeId 重构（已完成）
- 静态加载 `data/entities.json`（id 与项目原硬编码完全吻合：zombie=150、creeper=32… 已交叉验证），覆盖全部船型(oak_boat=89 等)与所有 mob，未知回落 150。原 switch 保留作兜底。
- 这是船/刷怪蛋能拿到正确网络实体类型 id 的关键。

### 补 6 类非拼图结构（已完成）
- `NonJigsawPlacer` switch 新增：`desert_pyramid`/`jungle_temple`(来自 jungle_pyramid.json 的 type)/`swamp_hut`/`end_city`/`ocean_monument`(来自 monument.json 的 type)/`woodland_mansion`(来自 mansion.json 的 type)。
- 6 类均为程序化简化版（外壳+内部+宝箱），非原版精确结构：
  - desert_pyramid：5 级砂岩金字塔 + 地下宝藏室（loot=`chests/desert_pyramid`）。
  - jungle_temple：9x9 石砖/苔石建筑 + 2 宝箱（`chests/jungle_temple`）。
  - swamp_hut：5x5 橡木小屋 + 炼药锅/工作台。
  - end_city：末地石平台 + 紫颂柱 + 末地烛。
  - ocean_monument：15x15 海晶石底座 + 四角塔 + 中央柱 + 海晶灯（loot=`chests/simple_dungeon`，因项目无 `chests/monument`）。
  - woodland_mansion：13x13 圆石外壳 + 深色橡木地板/屋顶 + 2 宝箱（`chests/woodland_mansion`）。
- 注意：end_city/woodland_mansion/ocean_monument 是否实际生成取决于各自 StructureSet 的生物群系闸口（pre-existing 基建），放置逻辑已就位。

### 本轮编译
- `mvn -o compile -DskipTests` BUILD SUCCESS（exit 0）。改动文件：`NetworkHandler.java`、`EntityManager.java`、`NonJigsawPlacer.java`。

---

## 生存模式功能完整性审计（2026-08-07 晚，用户要求核对整条生存链）

### 结论
- 用户列出的 10 步生存链（砍树→合成工具→打怪→挖矿→烧矿→做黑曜石→进入下界→找要塞→进入末地→打末影龙）**全部已实现且接线正确，可打通**。
- 14 项生存子系统审计：13 项已实现，唯一实质缺口是第 13 项（附魔台/铁砧/酿造台/锻造台）。

### 关键依赖核实（代码证据）
- 黑曜石：`FluidEngine.java:71-98` 水遇岩浆源→obsidian、流动岩浆→cobblestone，tick 驱动（非摆设）。
- 下界门：flint_and_steel(632)→tryIgniteNetherPortal(4639) 校验 4×5 黑曜石框填 nether_portal；维度旅行已接线（THE_NETHER/THE_END）。
- 要塞+末影之眼：`StructureManager.generateStronghold(177)` 用 STRONGHOLD_SPACING=48/SEP=12；`findNearest("stronghold")(208)` 用相同 spacing/key/offset → **定位与生成一致**，末影之眼正确指向要塞。
- 末地门激活+末影龙：tryActivateEndPortal(4909) 12 眼满点亮；EndDragonFight/EnderDragonEntity/EndCrystalEntity 完整（含 exit portal、dragon_egg、12000XP、终末诗）。

### 完整生存缺口（用户清单之外，需补全）
- 附魔台：ABSENT（`/enchant` 返回"待实现"，enchantment.json 已加载但未施加）。
- 铁砧：ABSENT（修复/重命名/合并附魔书）。
- 酿造台：ABSENT（力量/治疗/抗火/缓降药水下界与打龙必备）。
- 锻造台：ABSENT → netherite 无法获得（完整生存缺一环）。
- 后期功能：elytra 滑翔、潜影盒、末影箱（存储/机动）大概率缺失。

### 已知 bug / 风险
1. 挖掘服务端信任客户端 0x28 status==2 直接破坏，无速度校验 → insta-mine 作弊漏洞（仅控掉落不控破坏）。
2. 要塞疑似双重生成：`StructureManager.generateStronghold` 与 `NonJigsawPlacer.placeStronghold` 两条路径，需确认是否都执行（冗余，非崩溃）。
3. 附魔数据已加载但 inert（无施加逻辑）。
